person = {'name': 'Yakob', 'age': 34}
for key, value in person.items():
    print(key, value)

# Output:
# name Yakob
# age 34
# The items() method returns a view object that displays a list of a dictionary's key-value tuple pairs.
# The view object is dynamic and will reflect any changes made to the dictionary.   
